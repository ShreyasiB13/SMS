
@{
    ViewBag.Title = "LoggedIn";
}
@if (Session["Username"] != null)
{
   <h4>Hello @Session["Username"].ToString()</h4>
}

<h2>LoggedIn</h2>
<button type="button" onclick="location.href='@Url.Action("ViewDetails","Account")'">ViewTrainers</button>
